import React from "react";
import { View } from "react-native";
type Props = {
  title?: string;
  color?: string;
  icon?: string;
};

export const Header = () => {
  return <View style={{ width: "100%" }}>Header</View>;
};
